function Navbar(){
    return(
        <nav>
          
        </nav>
    );
}

//first we need to link this function to the main...to do that we first need to export this file and the import it into main file

export default Navbar          